{
  "name": "agent-bundle-demo-code-formatter-e2b",
  "private": true,
  "description": "Standalone code formatter demo using E2B sandbox",
  "scripts": {
    "build": "agent-bundle build",
    "dev": "agent-bundle dev",
    "start": "agent-bundle serve",
    "setup": "bash ./setup.sh"
  },
  "dependencies": {
    "agent-bundle": "^0.1.0"
  }
}
