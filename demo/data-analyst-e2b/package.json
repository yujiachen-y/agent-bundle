{
  "name": "agent-bundle-demo-data-analyst-e2b",
  "private": true,
  "description": "Standalone data analyst demo using E2B sandbox",
  "scripts": {
    "build": "agent-bundle build",
    "dev": "agent-bundle dev",
    "start": "agent-bundle serve",
    "setup": "bash ./setup.sh"
  },
  "dependencies": {
    "agent-bundle": "^0.1.0"
  }
}
