{
  "name": "agent-bundle-demo-financial-plugin",
  "private": true,
  "description": "Standalone financial plugin demo with marketplace integration",
  "scripts": {
    "build": "agent-bundle build",
    "dev": "agent-bundle dev",
    "start": "agent-bundle serve",
    "setup": "bash ./setup.sh"
  },
  "dependencies": {
    "agent-bundle": "^0.1.0"
  }
}
